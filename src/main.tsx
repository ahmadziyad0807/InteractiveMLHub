import React from 'react'
import ReactDOM from 'react-dom/client'
import MLShowcase from '../MLAlgorithmShowcase.tsx'
import '../globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MLShowcase />
  </React.StrictMode>,
)